# Copy to a host-level .env and populate through the CCE secret mechanism.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash
NEXT_PUBLIC_VERIQ_API_URL=https://api.veriq.example.cce.zw
VERIQ_ALLOWED_ORIGINS=https://veriq.example.cce.zw
